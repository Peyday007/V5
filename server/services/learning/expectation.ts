/**
 * What Brain expects from a deep dive when it launches one, stated once.
 *
 * Its own module so the launcher and the observer read one sentence without
 * the launcher importing the observer, which imports the launcher's module.
 */
export const DEEP_DIVE_EXPECTATION =
  'Research runs against published sources and establishes who pays for this opening, ' +
  'settling within the six-hour stall window.';
